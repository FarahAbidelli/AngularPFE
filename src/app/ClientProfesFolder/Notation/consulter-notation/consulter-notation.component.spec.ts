/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ConsulterNotationComponent } from './consulter-notation.component';

describe('ConsulterNotationComponent', () => {
  let component: ConsulterNotationComponent;
  let fixture: ComponentFixture<ConsulterNotationComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [ConsulterNotationComponent]
    });
    fixture = TestBed.createComponent(ConsulterNotationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
