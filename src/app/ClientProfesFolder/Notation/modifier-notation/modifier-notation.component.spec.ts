/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ModifierNotationComponent } from './modifier-notation.component';

describe('ModifierNotationComponent', () => {
  let component: ModifierNotationComponent;
  let fixture: ComponentFixture<ModifierNotationComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [ModifierNotationComponent]
    });
    fixture = TestBed.createComponent(ModifierNotationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
